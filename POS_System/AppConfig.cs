namespace POS_system
{
    // Minimal placeholder used by Program.Main.
    // Extend to load configuration from file or environment later.
    public static class AppConfig
    {
        public static void InitializeDatabase()
        {
            // Left intentionally empty so Program.Main compiles.
            // Add initialization logic here if needed (e.g., read config file).
        }
    }
}